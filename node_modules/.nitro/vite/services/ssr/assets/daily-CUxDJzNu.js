import { t as useProgress, u as todayKey } from "./store-BeK7TWEZ.js";
import { t as Button } from "./button-DTS8Rl4x.js";
import { l as SceneArt, u as Page } from "./router-CDCqzIHv.js";
import { t as Card } from "./card-D_EAS73j.js";
import { a as nextLesson, i as lessons } from "./content-DTcfiWqU.js";
import { useEffect } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/daily.tsx?tsr-split=component
function Daily() {
	const completed = useProgress((s) => s.completedLessons);
	const dailyDate = useProgress((s) => s.dailyDate);
	const dailyLessonId = useProgress((s) => s.dailyLessonId);
	const setDaily = useProgress((s) => s.setDailyLesson);
	const today = todayKey();
	useEffect(() => {
		if (dailyDate !== today) {
			const next = nextLesson(completed);
			setDaily(next.id, today);
		}
	}, [
		completed,
		dailyDate,
		setDaily,
		today
	]);
	const id = dailyDate === today && dailyLessonId ? dailyLessonId : nextLesson(completed).id;
	const lesson = lessons.find((l) => l.id === id) ?? nextLesson(completed);
	return /* @__PURE__ */ jsxs(Page, {
		title: "A little Japanese today",
		kicker: "Daily",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "text-sm leading-relaxed text-muted-foreground",
				children: "Listen → understand → repeat → use. Not twenty words. One situation, lived well."
			}),
			/* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted-foreground",
				children: "শুনুন → বুঝুন → বলুন → ব্যবহার করুন।"
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "overflow-hidden",
				children: [/* @__PURE__ */ jsx(SceneArt, { art: lesson.art }), /* @__PURE__ */ jsxs("div", {
					className: "space-y-2 p-5",
					children: [
						/* @__PURE__ */ jsx("p", {
							className: "text-xs font-medium uppercase tracking-wider text-muted-foreground",
							children: "Today’s situation"
						}),
						/* @__PURE__ */ jsx("h2", {
							className: "font-display text-2xl",
							children: lesson.title.en
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-sm text-muted-foreground",
							children: lesson.title.bn
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-sm leading-relaxed",
							children: lesson.teaser.en
						}),
						/* @__PURE__ */ jsx(Button, {
							asChild: true,
							className: "mt-2 w-full",
							size: "lg",
							children: /* @__PURE__ */ jsx(Link, {
								to: "/lesson/$id",
								params: { id: lesson.id },
								children: "Begin"
							})
						})
					]
				})]
			}),
			/* @__PURE__ */ jsx(Button, {
				asChild: true,
				variant: "outline",
				className: "w-full",
				children: /* @__PURE__ */ jsx(Link, {
					to: "/review",
					children: "Or review what you already lived"
				})
			})
		]
	});
}
//#endregion
export { Daily as component };
