import { l as shuffle, s as cn, t as useProgress } from "./store-BeK7TWEZ.js";
import { t as Button } from "./button-DTS8Rl4x.js";
import { l as SceneArt, o as AudioBar, s as speechUnlocked, u as Page } from "./router-CDCqzIHv.js";
import { t as Card } from "./card-D_EAS73j.js";
import { o as reachableSentences } from "./content-DTcfiWqU.js";
import { t as BiText } from "./bi-text-WG33CKno.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/listen.tsx?tsr-split=component
function Listen() {
	const completed = useProgress((s) => s.completedLessons);
	const autoPlay = useProgress((s) => s.autoPlay);
	const queue = useMemo(() => shuffle(reachableSentences(completed)).slice(0, 8), [completed]);
	const [i, setI] = useState(0);
	const [picked, setPicked] = useState(null);
	const item = queue[i];
	const options = useMemo(() => {
		if (!item) return [];
		const others = shuffle(queue.filter((s) => s.ja !== item.ja)).slice(0, 2);
		return shuffle([item, ...others]);
	}, [item, queue]);
	if (!item) return /* @__PURE__ */ jsx(Page, {
		title: "Listening",
		kicker: "Listen",
		children: /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted-foreground",
			children: "Live a situation first, then come back to hear it again."
		})
	});
	return /* @__PURE__ */ jsxs(Page, {
		title: "Listen first",
		kicker: "Listening",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "text-sm leading-relaxed text-muted-foreground",
				children: "Hear Japanese. Guess the situation. Meaning comes after."
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "overflow-hidden",
				children: [/* @__PURE__ */ jsx(SceneArt, { art: item.art }), /* @__PURE__ */ jsxs("div", {
					className: "space-y-4 p-5",
					children: [
						/* @__PURE__ */ jsx("p", {
							lang: "ja",
							className: "ja text-center text-2xl",
							children: item.ja
						}),
						/* @__PURE__ */ jsx(AudioBar, {
							ja: item.ja,
							auto: autoPlay && speechUnlocked()
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-sm",
							children: "What situation is this?"
						}),
						/* @__PURE__ */ jsx("div", {
							className: "space-y-2",
							children: options.map((o) => {
								const key = o.ja;
								const show = picked !== null;
								return /* @__PURE__ */ jsxs("button", {
									type: "button",
									disabled: picked !== null,
									onClick: () => setPicked(key),
									className: cn("w-full rounded-[var(--radius-lg)] bg-background px-4 py-3 text-left text-sm shadow-[var(--shadow-border)]", show && o.ja === item.ja && "bg-success/10", show && picked === key && o.ja !== item.ja && "bg-destructive/10"),
									children: [o.title.en, /* @__PURE__ */ jsx("span", {
										className: "mt-0.5 block text-xs text-muted-foreground",
										children: o.meaning.en
									})]
								}, key);
							})
						}),
						picked !== null ? /* @__PURE__ */ jsx(BiText, {
							text: item.meaning,
							revealed: true
						}) : null,
						picked !== null ? /* @__PURE__ */ jsx(Button, {
							className: "w-full",
							onClick: () => {
								setPicked(null);
								setI((i + 1) % queue.length);
							},
							children: "Next sound"
						}) : null
					]
				})]
			}),
			/* @__PURE__ */ jsxs("p", {
				className: "text-center text-xs tabular-nums text-muted-foreground",
				children: [
					i + 1,
					" / ",
					queue.length
				]
			})
		]
	});
}
//#endregion
export { Listen as component };
