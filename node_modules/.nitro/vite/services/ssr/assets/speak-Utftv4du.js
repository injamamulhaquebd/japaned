import { l as shuffle, t as useProgress } from "./store-BeK7TWEZ.js";
import { t as Button } from "./button-DTS8Rl4x.js";
import { o as AudioBar, s as speechUnlocked, u as Page } from "./router-CDCqzIHv.js";
import { t as Card } from "./card-D_EAS73j.js";
import { o as reachableSentences } from "./content-DTcfiWqU.js";
import { t as SpeakPanel } from "./speak-panel-CAPfiq5N.js";
import { t as BiText } from "./bi-text-WG33CKno.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/speak.tsx?tsr-split=component
function Speak() {
	const completed = useProgress((s) => s.completedLessons);
	const autoPlay = useProgress((s) => s.autoPlay);
	const showRomaji = useProgress((s) => s.showRomaji);
	const queue = useMemo(() => shuffle(reachableSentences(completed)).slice(0, 10), [completed]);
	const [i, setI] = useState(0);
	const item = queue[i];
	if (!item) return /* @__PURE__ */ jsx(Page, {
		title: "Speaking",
		kicker: "Speak",
		children: /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted-foreground",
			children: "Hear a lesson first, then come copy the sounds."
		})
	});
	return /* @__PURE__ */ jsxs(Page, {
		title: "Speak like a child",
		kicker: "Speaking",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "text-sm leading-relaxed text-muted-foreground",
				children: "Small mistakes are allowed. Confidence first. Optional microphone — the lesson still works without it."
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "space-y-4 p-5",
				children: [
					/* @__PURE__ */ jsx("p", {
						lang: "ja",
						className: "ja text-center text-3xl",
						children: item.ja
					}),
					showRomaji ? /* @__PURE__ */ jsx("p", {
						className: "text-center text-sm text-muted-foreground",
						children: item.romaji
					}) : null,
					/* @__PURE__ */ jsx(AudioBar, {
						ja: item.ja,
						auto: autoPlay && speechUnlocked(),
						size: "lg"
					}),
					/* @__PURE__ */ jsx(SpeakPanel, { ja: item.ja }),
					/* @__PURE__ */ jsx(BiText, {
						text: item.meaning,
						revealed: true
					}),
					/* @__PURE__ */ jsx(Button, {
						className: "w-full",
						onClick: () => setI((i + 1) % queue.length),
						children: "Next sentence"
					})
				]
			}),
			/* @__PURE__ */ jsxs("p", {
				className: "text-center text-xs tabular-nums text-muted-foreground",
				children: [
					i + 1,
					" / ",
					queue.length
				]
			})
		]
	});
}
//#endregion
export { Speak as component };
