import { t as useProgress } from "./store-BeK7TWEZ.js";
import { t as Button } from "./button-DTS8Rl4x.js";
import { n as Route } from "./router-CDCqzIHv.js";
import { n as getLesson } from "./content-DTcfiWqU.js";
import { n as LessonPlayer } from "./player-BVHMXnTe.js";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/lesson.$id.tsx?tsr-split=component
function LessonPage() {
	const { id } = Route.useParams();
	const lesson = getLesson(id);
	const navigate = useNavigate();
	const complete = useProgress((s) => s.completeLesson);
	if (!lesson) return /* @__PURE__ */ jsxs("main", {
		className: "flex min-h-dvh flex-col items-center justify-center gap-3 px-6 text-center",
		children: [/* @__PURE__ */ jsx("p", { children: "That situation is not here." }), /* @__PURE__ */ jsx(Button, {
			onClick: () => navigate({ to: "/learn" }),
			children: "Back to learn"
		})]
	});
	return /* @__PURE__ */ jsx(LessonPlayer, {
		title: lesson.title,
		art: lesson.art,
		steps: lesson.steps,
		onComplete: () => complete(lesson.id),
		onExit: () => navigate({ to: "/learn" })
	});
}
//#endregion
export { LessonPage as component };
